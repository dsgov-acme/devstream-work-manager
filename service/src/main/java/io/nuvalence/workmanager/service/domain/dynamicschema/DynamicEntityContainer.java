package io.nuvalence.workmanager.service.domain.dynamicschema;

/**
 * INterface for objects that have a dynamic data component.
 */
public interface DynamicEntityContainer {
    DynamicEntity getData();
}
